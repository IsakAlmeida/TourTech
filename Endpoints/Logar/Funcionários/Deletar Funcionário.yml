info:
  name: Deletar Funcionário
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{PROD}}/funcionarios/deletar"
  body:
    type: json
    data: |-
      {
        "id_usuario": 2,
        "idEmpresa":  1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
