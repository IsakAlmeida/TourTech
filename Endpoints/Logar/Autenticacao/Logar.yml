info:
  name: Logar
  type: http
  seq: 1

http:
  method: POST
  url: "{{PROD}}/autenticacao/logar"
  body:
    type: json
    data: |-
      {
        "emailServer": "bruno@gmail.com",
        "senhaServer": "senha123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
