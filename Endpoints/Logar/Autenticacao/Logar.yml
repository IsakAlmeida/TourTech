info:
  name: Logar
  type: http
  seq: 1

http:
  method: POST
  url: "{{localhost}}/autenticacao/logar"
  body:
    type: json
    data: |-
      {
        "emailServer": "bruno@gmail.com",
        "senhaServer": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
