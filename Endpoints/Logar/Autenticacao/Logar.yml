info:
  name: Logar
  type: http
  seq: 1

http:
  method: POST
  url: "{{PROD}}/autenticacao/logar"
  body:
    type: json
    data: |-
      {
        "emailServer": "bruno@gmail.com",
        "senhaServer": "asdf1234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
