info:
  name: Verificar Token
  type: http
  seq: 2

http:
  method: POST
  url: "{{PROD}}/autenticacao/verificarToken"
  body:
    type: json
    data: |-
      {
        "tokenServer": "ABC123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
