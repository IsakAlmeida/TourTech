info:
  name: Gráfico Pais
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/dashboard//chartInterNacionalEstado
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
