info:
  name: KPI de mês moda nacional
  type: http
  seq: 1

http:
  method: GET
  url: "{{PROD}}/dashboard/kpiModaNacional"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
