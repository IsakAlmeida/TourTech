info:
  name: Buscar Estabelecimento
  type: http
  seq: 4

http:
  method: GET
  url: "{{PROD}}/estabelecimento/buscarEstabelecimento/3/hospedagem"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
