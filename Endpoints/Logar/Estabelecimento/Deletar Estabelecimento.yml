info:
  name: Deletar Estabelecimento
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{PROD}}/estabelecimento/deletar"
  body:
    type: json
    data: |-
      {
        "id":   400,
        "tipo": "hospedagem"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
