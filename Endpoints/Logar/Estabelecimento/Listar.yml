info:
  name: Listar
  type: http
  seq: 1

http:
  method: GET
  url: "{{PROD}}/estabelecimento/listar"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
