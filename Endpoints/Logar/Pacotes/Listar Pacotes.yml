info:
  name: Listar Pacotes
  type: http
  seq: 1

http:
  method: GET
  url: "{{PROD}}/pacote/listarPacotes"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
