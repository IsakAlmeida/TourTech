info:
  name: Buscar Pacote
  type: http
  seq: 6

http:
  method: GET
  url: "{{PROD}}/pacote/buscar/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
